# Copia este archivo como ".env" (sin ".example") y reemplaza los
# valores por credenciales propias antes de ejecutar "docker compose up".
# El archivo ".env" real NUNCA debe subirse a GitHub (ver .gitignore).

DB_NAME=sample
DB_USER=sampleuser
DB_PASSWORD=CAMBIA_ESTA_CLAVE
DB_ROOT_PASSWORD=CAMBIA_ESTA_CLAVE_ROOT
